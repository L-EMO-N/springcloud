server:
  port: 9090
  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/
    instance:
      prefer-ip-address: true
spring:
  application:
    name: microservice-consumer-movie-feign-customizing

logging:
  level:
    com.lemon.springcloud.consumer.feignclient.UserFeignClient: DEBUG